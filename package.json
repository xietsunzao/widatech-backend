{
  "name": "widatech-testcode",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0"
  },
  "scripts": {
    "start": "bun run ./src/main.ts",
    "dev": "bun --watch --smol run ./src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build ./server.ts --outdir ./dist --target bun",
    "prsima:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "express": "^4.21.2",
    "mysql2": "^3.11.5",
    "prisma": "^6.0.1",
    "zod": "^3.24.1"
  }
}